import React from 'react';
import { View, StyleSheet } from 'react-native';
import LoginScreen from './src/screens/LoginScreen'; // Assuming the LoginScreen component is in the same directory


const App = () => {
  return (
    <View>
      <LoginScreen />
    </View>
  );
};

export default App;